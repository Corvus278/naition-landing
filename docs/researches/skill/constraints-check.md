# Проверка после внедрения

Проверено в Chrome при 1280×720 (разрешение всех 101 визита) на локальном сервере `php -S 127.0.0.1:8765`, плюс контроль вёрстки при 390 px.

## Чеклист «нельзя менять»

| Запрет (из `site-constraints.md`) | Факт после правок | Итог |
|-----------------------------------|-------------------|------|
| Форма `#registration-form` с `action="api/submit.php"` | id на месте, `action="api/submit.php"`, `method="post"` | **pass** |
| Поля `name`, `phone`, `email`, `purpose` | `["name","phone","email","purpose"]`, `purpose` остался `<textarea required>` | **pass** |
| Скрытое поле `bot_session_id` | в исходном коде отсутствовало (legacy, удаляется миграцией в `api/db.php`), не добавлялось и не удалялось | **pass** |
| `<script src="api/visit.php">` в `<head>` | присутствует в `<head>` | **pass** |
| Класс `.btn-register` | 3 кнопки, как было; поведение (скролл к `#registration`) сохранено | **pass** |
| Класс `.pricing-section` | 1 секция | **pass** |
| Класс `.program-module` | 6 элементов, тег остался `<article>` | **pass** |
| Класс `.program-list` | 1 контейнер | **pass** |
| PHP в `api/`, `sql/schema.sql` | не изменялись (`git status` — только `index.php`, `css/style.css`, `js/main.js`, `docs/researches/skill/**`) | **pass** |
| `data/app.db` не коммитить | в `.gitignore`, не добавлялся; тестовая заявка smoke-теста удалена из локальной БД | **pass** |

## Structural audit — закрыто?

| Наблюдение (этап 0) | Рекомендация | Status |
|---------------------|--------------|--------|
| B1. Карточки имитируют кликабельность без отклика | R2 — снят `cursor: pointer` и hover-подъём у `.feature-card`, `.program-module`, `.injury-card`; реальный интерактив (`summary`, чипы, кнопки) сохранил курсор-руку | implemented |
| B2. Ни одной точки действия на первых 85% страницы | R1 + R4 — кнопка на первом экране (y=472 вместо 7266) и блок с действием после программы (y=3062) | implemented |
| B3. Выбранный тариф не доезжает до формы | R7 — клик по «Записаться» подставляет тариф и цену в сводку над полями и подсвечивает выбранную карточку | implemented |
| B4. Нет доказательств и ни слова о персональных данных | R6 + R9 — блок цифр и лицензии у инструкторов, строка о персональных данных под кнопкой, три подтверждения под формой | implemented |
| B5. Программа перегружает середину страницы | R3 + R5 — модули свёрнуты до короткой сути с раскрытием по клику (видимый текст 4388 → 1130 знаков, высота списка 2081 → 1159 px), блок «Виды травм» переписан без дублирования программы | implemented |

## Рекомендации

| id | layer | status | notes |
|----|-------|--------|-------|
| R1 | cluster (кластер 3) | implemented | новый заголовок-оффер, цена «от 4 900 ₽» и срок ответа рядом с кнопкой, кнопка выше сгиба (y=472 при вьюпорте 720) |
| R2 | structural (B1) | implemented | ложная кликабельность снята в CSS |
| R3 | structural (B5) | implemented | `<details class="module-more">` внутри каждого `.program-module`; класс и тег модуля не тронуты |
| R4 | structural (B2) | implemented | блок «Занять место» после программы, якорь `a.btn`, не `.btn-register` |
| R5 | cluster (кластер 2) | implemented | «Виды травм и состояний» → «После курса вы сможете», карточки сокращены |
| R6 | structural (B4) | implemented | 4 факта у инструкторов (выпускники, оценка, лицензия, размер группы) |
| R7 | structural (B3) | implemented | `data-tariff`/`data-price` + обработчик в `js/main.js` |
| R8 | cluster (кластер 1) | implemented | 4 чипа быстрого выбора цели заполняют то же поле `purpose`; textarea уменьшена 140 → 76 px, добавлены плейсхолдеры, подпись кнопки «Записаться на курс» |
| R9 | cluster (кластер 1) | implemented | сводка заказа (тариф, цена, дата, время, место) + строка о персональных данных + три подтверждения под формой |
| R10 | structural (B2, вариант) | deferred | липкая панель не добавлена: `position: fixed` рискует перехватывать клики симулятора; B2 закрыт через R1 и R4 |

## Интерактив после правок

| Элемент | Ожидание | Факт | Итог |
|---------|----------|------|------|
| `.btn-hero` (первый экран) | якорь ведёт к форме | `href="#registration"`, цель существует | **pass** |
| `.btn-inline` (после программы) | якорь ведёт к форме | `href="#registration"`, цель существует | **pass** |
| `.btn-register` × 3 | скролл к `#registration` + перенос тарифа | после клика `scrollY=7073`, отступ секции 0 px, сводка показывает «Корпоративный — 12 900 ₽» / «Базовый — 4 900 ₽» по нажатой карточке | **pass** |
| `.pricing-card` | подсветка выбранного тарифа | класс `selected` появляется у нажатой карточки | **pass** |
| `.module-more summary` | клик раскрывает подробности модуля | `details.open` переключается | **pass** |
| `.purpose-chip` × 4 | клик заполняет поле `purpose` | значение подставилось: «Хочу защитить семью и детей», чип помечается `selected` | **pass** |
| `#registration-form` | отправка проходит и сохраняется | `{ok:true}`, сообщение «Заявка успешно отправлена…», класс `form-message success`, запись появилась в `orders` (удалена после теста) | **pass** |
| Пустое поле `purpose` | отправка не проходит (сервер требует непустое значение) | `checkValidity() === false`, браузер блокирует отправку | **pass** |
| Бывшие ложные аффордансы `.feature-card`, `.program-module`, `.injury-card` | не выглядят кликабельными | `cursor: default`, hover-подъём убран | **pass** |
| Горизонтальный скролл при 390 px | отсутствует | `scrollWidth === clientWidth`, элементов за границей нет | **pass** |

## Изменённые файлы

- `index.php` — первый экран (оффер, цена, кнопка, счётчик мест); программа свёрнута до сути + `<details>`; блок действия после программы; «Виды травм» переписан в «После курса вы сможете»; доказательства у инструкторов; `data-tariff`/`data-price` и уточнённые подписи кнопок тарифов; форма — сводка заказа, чипы выбора цели, плейсхолдеры, подпись кнопки, строка о персональных данных, подтверждения.
- `css/style.css` — снята ложная кликабельность карточек; стили hero-CTA, `details`, inline-CTA, блока доказательств, сводки заказа, чипов, акцентных кнопок; `textarea` 140 → 76 px; hero компактнее.
- `js/main.js` — перенос выбранного тарифа в сводку и подсветка карточки; чипы заполняют `purpose` (с `input`/`change`), синхронизация чипов с ручным вводом, сброс после успешной отправки. Существующий обработчик `.btn-register` со скроллом сохранён.
- `docs/researches/skill/**` — артефакты анализа (`landing-map.json`, `hypotheses.md`, `site-constraints.md`, `recommendations.md`, `constraints-check.md`, `work/visits/*.json` — 101 файл).

## Как изменилась геометрия страницы

| Метрика | До | После |
|---------|----|-------|
| Высота страницы | 8540 px | 8634 px |
| Первый интерактивный элемент | y=7266 (85% высоты) | y=472 (в первом экране) |
| Первое упоминание цены | y=6720 (79%) | первый экран, рядом с кнопкой |
| Видимый текст программы | 4388 знаков / 2081 px | 1130 знаков / 1159 px |
| Точки перехода к форме | 3 (только в тарифах) | 5 (первый экран, после программы, 3 в тарифах) |

## Workflow (из site_dir)

- [ ] **Redeploy** в control-panel (данные в БД сбрасываются) — выполняет пользователь, агенту недоступно.
- [ ] **Run visits simulator** (100 визитов) после редеплоя.
- [ ] Сравнить конверсию до и после: базовая линия — **4 конверсии из 101 визита (3,96%)** по данным от 2026-07-23.
- [ ] Свериться с `https://<домен>/api/export.php` и `admin/orders.php`.

Отдельно: сейчас на боевом домене развёрнута ветка `feature/grow-conversion-without-skill`. Чтобы измерять именно эти правки, перед прогоном симулятора нужно задеплоить ветку `feature/grow-conversion-with-skill`.

## Итог

- [x] Все запреты соблюдены (10 из 10 пунктов — pass).
- [x] Топ-3 кластера внедрены (R1, R5, R8+R9).
- [x] Structural audit: 5 наблюдений из 5 закрыты (B1–B5); R10 осознанно отложен с объяснением.
- [x] Интерактив из карты лендинга проверен: 10 из 10 проверок — pass.
